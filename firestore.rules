rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all - since we use Firebase Admin SDK for all server-side operations
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
